---
description: 'A link that receives base-react props and outputs a React-router link'
labels: ['ui', 'react-router', 'link']
---

import { Link } from './link';

### Usage

``` ts
<Link>react router link</Link>
```

This component is used to inject a `React-router` link via React's context api, and should not be styled directly.

It assumes it is wrapped by a React-router `<Router>` in order to work properly.
