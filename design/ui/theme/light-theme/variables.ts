import { BaseThemeType } from '@teambit/design.ui.theme.base-theme';

/**
 * tokens for a light theme.
 */
export const lightTheme: Partial<BaseThemeType> = {
  onPrimaryColor: '#FFFFFF',
  primaryColor: '#6C5CE7',
};
