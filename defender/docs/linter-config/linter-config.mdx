# Linter configuration

## extensionFormats

```ts
extensionFormats: string[]
```

Defines which files should go through linting. For example: `"extensionFormats": ['.ts', '.tsx', '.js']`

## fixTypes

```ts
fixTypes: ('problem' | 'suggestion' | 'layout')[]
```

Defines which types to fix.
