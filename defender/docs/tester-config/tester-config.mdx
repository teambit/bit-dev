# Tester configuration

## watchOnStart

```ts
watchOnStart: boolean;
```

Determines whether tests should run whenever a component changes (relevant only when running `bit start`).

## patterns

```ts
patterns: string[]
```

Sets the filename patterns for test files.
