import { ComponentCardDisplay } from '@teambit/components.blocks.component-card-display';

# Creating Aspects

Aspects are components used to extend Bit. Every Aspect is a component, which is using the Aspect environment.

## Creating an Aspect

Create a basic Aspect with using the official aspect env template.

```
bit create aspect my-aspect
```

Your new component incldues four new files: `my-aspect.aspect.ts`, `my-aspect.main-runtime.ts` and regular documantation, compositions and test files.

## Defining the API

Every Aspect can expose APIs for others to use, whether they are an other Aspect or an external consumer.
Adding an API is as simple as adding a method to a class.

To start adding APIs to your Aspect, head over your `main.runtime` file, and add new methods to it. 
Here is an example for a basic `sayHello()` API.

```ts
export class MyAspectMain {
  sayHello() {
    return 'hello world!';
  }

  static slots = [];
  static dependencies = [];
  static runtime = MainRuntime;
  static async provider() {
    return new MyAspectMain();
  }
}
```

## Using configuration

```ts
import { MainRuntime } from '@teambit/cli';

export class MyAspectMain {
  sayHello() {
    return 'hello world!';
  }

  static slots = [];
  static dependencies = [];
  static runtime = MainRuntime;
  static async provider() {
    return new MyAspectMain();
  }
}
```

Aspects are designed to be API first. We are using programmatic APIs as a base and then composing composed to more advanced interfaces 
like UI, RESTful, GraphQL or anything else that might be of interest. 


## Using dependencies

Every Aspect can use APIs provided by other Aspects. You can use one of our  

<ComponentCardDisplay componentIds={[
  'teambit.harmony/graphql',
  'teambit.harmony/express',
]} />


```ts
// import the CLI aspect and the type of CLI main runtime.
import { MainRuntime, CLIMain, CLIAspect } from '@teambit/cli';

export class MyAspectMain {
  // your aspect's API.
  sayHello() {
    return 'hello world!';
  }

  static slots = [];
  // define CLIAspect as a dependency for your Aspect.
  static dependencies = [CLIAspect];
  static runtime = MainRuntime;

  // CLI in the correspoding runtime will be injected to your provider.
  static async provider([cli]: [CLIMain]) {
    // register a new command into the Bit CLI.
    cli.register([new MyCommand()]);
    return new MyAspectMain();
  }
}
```

## Using APIs

<ComponentCardDisplay componentIds={[
  'teambit.harmony/graphql',
  'teambit.harmony/express',
  'teambit.harmony/cli',
]} />


## Using slots


## Using runtimes 


