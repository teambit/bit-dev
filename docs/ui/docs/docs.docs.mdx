---
description: 'A component-based documentation section'
labels: ['docs', 'page', 'routes']
---

import { MemoryRouter, UNSAFE_LocationContext } from 'react-router-dom';
import { SidebarProvider } from '@teambit/design.ui.sidebar.sidebar-context';
import { RouterProvider } from '@teambit/base-react.navigation.router-context';
import { reactRouterAdapter } from '@teambit/ui-foundation.ui.navigation.react-router.routing-adapter';
import { Docs } from './docs';

`Docs` is a component-based documentation section built in React. It is a React component you can use in your website's components.

The 'Docs' component renders the docs page (MDX) with a sidebar to its left.  
This component receives and renders all the Routes and makes them available in ReactRouter.

### Component usage

```tsx live
() => {
  const primaryRoutes = [
    {
      path: 'quick-start',
      icon: 'lightning',
      title: 'Quick Start',
      description: 'Quick Start',
      component: <div>My quick start content...</div>,
    },
    {
      path: 'thinking-in-components',
      icon: 'Lightbulb-thinking',
      title: 'Thinking in components',
      component: <div>Thinking in components contnet</div>,
    },
  ];
  const routes = [
    {
      path: 'installation',
      title: 'Installation',
      component: <div />,
      children: [
        {
          path: 'start-bit-project',
          title: 'Start a new Bit Workspace',
          component: <div>start bit project contnet</div>,
        },
      ],
    },
    {
      path: 'components',
      title: 'Components',
      component: <div />,
      children: [
        {
          path: 'creating-components',
          title: 'Creating components',
          component: <div>creating compoents contnet</div>,
        },
        {
          path: 'component-config',
          title: 'Component Configuration',
          component: <div>component config</div>,
        },
      ],
    },
  ];

  return (
    <RouterProvider value={reactRouterAdapter}>
      <SidebarProvider>
        {/* @ts-ignore @HACK, cannot nest MemoryRouter inside another router - see https://github.com/remix-run/react-router/issues/7375 */}
        <UNSAFE_LocationContext.Provider value={null}>
          <MemoryRouter>
            <Docs baseUrl="/" contents={[{ routes }]} primaryLinks={primaryRoutes} />
          </MemoryRouter>
        </UNSAFE_LocationContext.Provider>
      </SidebarProvider>
    </RouterProvider>
  );
};
```
