export const bitmap = `/**
* .bitmap file maps components to their directories in the workspace. 
* Track this file when collaborating on a workspace using git so as to record the status and version of components in that workspace.
*
*  .bitmap is maintained by Bit and should not be modified manually!
*/

{
    "$schema-version": "14.9.0"
    /* since this workspace is new, there are no components registered */
}`;
