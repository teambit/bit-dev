#### Dependencies

The 'dependencies' property lists the component regular dependencies.

`@teambit/design.inputs.input-text@0.0.2` is listed as a dependency of this component since a relevant 'import' statement was detected in this component's files.
This dependency is labeled as a component (`(component)`).

See the [Dependency policy and dependency resolution](#dependency-policy-and-dependency-resolution) section to learn how Bit resolves the dependency type and version.
