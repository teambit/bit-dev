import React from 'react';
import { ExpandCode } from '@teambit/community.ui.expand-code';
import { BitScope } from './commands';

export const ExpandedScope = () => <ExpandCode snippet={BitScope} />;
