---
description: 'how to compose a build task to various places.'
labels: ['build', 'docs', 'envs']
---

build tasks are decoupled from an env or an aspect, so they can be composed via either of them.
