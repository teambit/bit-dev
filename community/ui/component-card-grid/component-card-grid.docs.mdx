---
description: 'ComponentCardGrid'
labels: ['label1', 'label2', 'label3']
---

import { ComponentCardGrid } from './component-card-grid';
import { FigmaEmbed } from '@teambit/design.embeds.figma';

// Add a description of your component
Detailed description of the component.

// Show how to use the component if not using live playground

### Component Usage

```js
<ComponentCardGrid text="hello" />;
import { mockComponentCards } from '@teambit/community.entity.component-cards';

<ComponentCardGrid componentCards={mockComponentCards} />;
```

// Describe what is shown in the live playground

### Shows a text of Hello

```js live
<ComponentCardGrid
  componentCards={[
    {
      id: 'teambit.community/ui/homepage/hero@1.0.1',
      version: '2.3.0',
      description: 'my lovely component',
      envIcon: 'https://static.bit.dev/brands/logo-react.svg',
      preview: 'button'
    },
    {
      id: 'teambit.community/ui/homepage/hero@1.0.1',
      version: '2.3.0',
      description: 'my lovely component',
      envIcon: 'https://static.bit.dev/brands/logo-react.svg',
      preview: 'button'
    }
  ]}
/>
```

Add more live playground examples if it makes sense

### Design

// add the url to Figma file if there are any design elements or remove if not using

<FigmaEmbed src="" />
