---
description: 'A Browser wrapper image.'
labels: ['browser', 'image']
---

import { Browser } from './browser';
import scopeUi from './scope-ui.png';

A basic div that renders a browser wrapper image on top and the `children` element after it.

### Component Usage

```js live
<Browser style={{ width: 400 }}>
  <img src={scopeUi} width="100%" />
</Browser>
```
