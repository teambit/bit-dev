---
description: 'A bubble graph mocks component.'
labels: ['mock', 'bubble', 'graph']
---

import ReactJson from 'react-json-view';
import { BubbleGraph } from '@teambit/community.ui.graph.bubble-graph';
import { ComponentBubbleNonInteractive } from '@teambit/community.ui.graph.component-bubble-non-interactive';
import { mockExplorePluginsGraph, mockCreatePluginsGraph } from './bubble-graph-mocks';

## Mock functions for rendering bubble graph

Mock data that renders the Bubble graph of the Explore section in plugins page.

### Component usage

For example, you can send `mockExplorePluginsGraph` or `mockCreatePluginsGraph` function to get different graph.

```js live
<BubbleGraph Node={ComponentBubbleNonInteractive} nodes={mockExplorePluginsGraph()} />
```
