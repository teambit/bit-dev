---
description: 'A React hook that handle the use of local storage.'
labels: ['react', 'hook', 'local storage']
---

import { useLocalStorage } from './use-local-storage';

The hook save and get value from local storage and handle changes using effect hook.

```js live
() => {
  const [value, setValue] = useLocalStorage('my-key', '');
  return <input value={value} onChange={(e) => setValue(e.target.value)} />;
};
```
