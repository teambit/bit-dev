---
description: 'Strip of Images with animation'
labels: ['image', 'strip']
---

import { ImageStrip } from './image-strip';
import { FigmaEmbed } from '@teambit/design.embeds.figma';
import { clientLogos } from '@teambit/community.entity.images';

## Overview

A strip of images with gentle animation. Lazy loading is added by default for all images but can be overridden with the `loading` prop.

### Component usage

Import some mock data from an external source and pass it to the `images` prop.

```js
import { clientLogos } from '@teambit/community.entity.images';

<ImageStrip data-testid="test-image-strip" logos={clientLogos} />;
```

### Live Playgournd

Add an array of images to the `images` prop.

```js live
<ImageStrip
  images={[
    {
      src: 'homepage-enterprise-logos/Dell.svg',
      alt: 'dell'
    },
    {
      src: 'homepage-enterprise-logos/Carlsberg-1.svg',
      alt: 'Carlsberg'
    }
  ]}
/>
```

### Design

<FigmaEmbed src="https://www.figma.com/embed?embed_host=share&url=https%3A%2F%2Fwww.figma.com%2Ffile%2FctC1w9YhJvqaO1EZ08gLOV%2FCommunity%3Fnode-id%3D5%253A488" />
