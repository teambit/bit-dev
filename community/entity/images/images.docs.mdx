---
description: 'Entity component for Images'
labels: ['mock', 'data', 'entity', 'types']
---

import { Images } from './images';

Array of types for an image

```js
alt: string;
src: string;
loading?: 'lazy' | 'eager';
```

## Mock Json

Mock data for images.

```json
export const clientLogos = [
  {
    src: 'https://static.bit.dev/homepage-enterprise-logos/Dell.svg',
    alt: 'Dell'
  },
  {
    src: 'https://static.bit.dev/homepage-enterprise-logos/ebay.svg',
    alt: 'ebay'
  },
];
```
