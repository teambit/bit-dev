---
description: 'Entity component for Bit use-cases.'
labels: ['mock', 'data', 'entity', 'types']
---

import { LeftRight } from '@teambit/design.ui.layouts.sections.left-right';
import { RenderUseCaseCodeSnippet } from './example/render-use-case-snippet.demo';
// import { UseCasesCardGrid } from '@teambit/community.ui.use-cases.use-cases-card-grid';
// import { useCases } from './use-cases.mocks';
import { RenderUseCasesMock } from './use-cases.compositions';

This component describes and demonstrates the "use-case" object. It serves as the source of truth for components sending and receiving "use-cases" data.

### Test components with mock data

Use the mock data provided by this component to test your components:

<LeftRight left={<RenderUseCaseCodeSnippet />} right={<RenderUseCasesMock />} />

### Create a new use-case object

Use this component to create a new use-case and validate it follows the right schema:

```ts
import { UseCases } from '@teambit/community.entity.use-cases';
import { title, text, image, alt, href } from './my-use-case-info';

const myUseCase = new UseCases(title, text, image, alt, href);
```

### Type data transferred via network requests

```ts
import type { UseCasesFromApi } from '@teambit/community.entity.use-cases';

async function fetchUseCase(): Promise<UseCasesFromApi> {
  // ...
}
```
