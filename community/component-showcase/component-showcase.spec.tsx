import React from "react";
import { render } from "@testing-library/react";
import { BasicComponentShowcase } from "./component-showcase.composition";

it("should render with the correct text", () => {
  expect(true).toBeTruthy();
});
