---
description: 'How to set and use Component Development Environments.'
labels: ['docs', 'envs', 'workspace']
---

Envs allows to control configuration and build pipelines for components. The `env` command controls the component config.

This component explains how to use the `env` command to manage development.

